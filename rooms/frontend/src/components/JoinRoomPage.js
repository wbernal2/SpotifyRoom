import React from "react";

export default function JoinRoomPage() {
  return (
    <div style={{ padding: "1rem", fontFamily: "sans-serif" }}>
      <h1 style={{ color: "blue" }}>🎉 JOIN ROOM PAGE 🎉</h1>
      <p>✅ This is the Join Room page. React is working, and the page loaded successfully.</p>
    </div>
  );
}
